# Example configuration (do not commit your local config).
#
# This project reads environment variables. You can either:
# - export them in your shell profile (~/.bashrc, ~/.zshrc), or
# - source a local file before running tools (e.g. source ~/.config/ubuntu-system-tools/config.env)

TOWER_BASE="$HOME/Documents/tower-notes"
PROJECTS_DIR="$HOME/Progetti"
LOG_DIR="$HOME/Documents/tower-notes/tower/logs"
